# Optional TLS reverse proxy (set LEOVEE_DOMAIN in Caddy environment).
{
  email admin@leovee.example.com
}

:80 {
  respond "Configure LEOVEE_DOMAIN for TLS" 404
}

# Example production host block (uncomment and set domain):
# leovee.example.com {
#   encode gzip
#   handle /api/* {
#     reverse_proxy api:8000
#   }
#   handle {
#     reverse_proxy web:80
#   }
#   header {
#     Strict-Transport-Security "max-age=31536000; includeSubDomains"
#     X-Content-Type-Options "nosniff"
#     X-Frame-Options "DENY"
#   }
# }
