# Golden fixtures

Reference outputs captured from the AiChart TypeScript implementation, one
directory per engine, one JSON file per fixture.

**Do not hand-write these.** A hand-written expectation pins the port to
whatever the port already does, which is the opposite of what they are for.
Capture them from the reference implementation as described in
`docs/PORTING.md`, then commit the JSON.
